name: CI/CD Production Release

on:
  workflow_dispatch:
jobs:
  push-to-production:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Authenticate GitHub CLI
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "$GH_TOKEN" | gh auth login --with-token

      - name: Generate Release Notes (Find Included PRs)
        id: release_notes
        run: |
          echo "Fetching differences between master and staging..."
          git fetch origin master:master --quiet
          
          # Get a beautifully formatted list of PRs merged into staging since last master sync
          PR_LIST=$(git log origin/master..origin/staging --merges --oneline --grep="Merge pull request" --format="* %s")
          
          if [ -z "$PR_LIST" ]; then
            # Fallback if squashed commits were used instead of explicit merge commits
            PR_LIST=$(git log origin/master..origin/staging --oneline --format="* %s" | grep -E '#[0-9]+')
          fi
          
          if [ -z "$PR_LIST" ]; then
             PR_LIST="* Direct commits or minor updates found. No explicit pull request titles parsed."
          fi

          # Clear multiline output parsing strings safely for GitHub Actions outputs
          EOF=$(dd if=/dev/urandom bs=15 count=1 2>/dev/null | base64)
          echo "prs<<$EOF" >> $GITHUB_OUTPUT
          echo "$PR_LIST" >> $GITHUB_OUTPUT
          echo "$EOF" >> $GITHUB_OUTPUT

      - name: Log PRs to GitHub Actions Console
        run: |
          echo "========================================="
          echo "🚀 PULL REQUESTS BEING MERGED TO MASTER:"
          echo "========================================="
          echo "${{ steps.release_notes.outputs.prs }}"
          echo "========================================="

      - name: Create and Automatically Merge Production PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Check if there are actual diffs between staging and master
          DIFF_COUNT=$(git rev-list --count origin/master..origin/staging)
          if [ "$DIFF_COUNT" -eq "0" ]; then
            echo "Master branch is already perfectly up to date with Staging."
            exit 0
          fi

          PR_BODY="### 🚀 Automated Master Release
          This PR merges the latest updates from \`staging\` into \`master\`.

          #### 📦 Features & Fixes included in this release:
          ${{ steps.release_notes.outputs.prs }}"

          echo "Creating Pull Request..."
          PR_URL=$(gh pr create \
            --base master \
            --head staging \
            --title "Release: Sync Staging to Master 🚀" \
            --body "$PR_BODY")
          
          echo "PR Created successfully: $PR_URL"
          
          # echo "Merging Pull Request immediately..."
          # gh pr merge "$PR_URL" --merge --delete-branch=false